name: first_worklfow
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: |
        echo "from build"
        echo "second line from first workflow"
