name: first.worklfow
on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: |
        echo "from build"
        echo ${{ tojson(github) }}
#        echo "second line from first workflow"
