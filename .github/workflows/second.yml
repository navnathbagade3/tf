name: second_workflow
on:
  workflow_run: 
    workflows: ['first_worklfow']
    types: 
      - completed
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploying
        run: echo "deploying"
